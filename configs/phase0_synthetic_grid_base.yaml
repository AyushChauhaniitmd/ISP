experiment_name: phase0_synthetic_grid
seed: 20260910

data:
  backend: synthetic
  n_clients: 12
  samples_per_client: 80
  n_features: 8
  heterogeneity: 0.20
  test_samples: 600

federated:
  rounds: 12
  local_epochs: 1
  local_batch_size: 16
  learning_rate: 0.15
  client_sample_rate: 0.5

privacy:
  enabled: true
  population_size: 12
  delta: 1.0e-5
  clip_norm: 1.0
  noise_multiplier: 1.1
  sampling: poisson
  release_count: 1

deletion:
  kind: client
  client_id: 2

unlearning:
  method: finetune_retained
  rounds: 4

output:
  root: results
