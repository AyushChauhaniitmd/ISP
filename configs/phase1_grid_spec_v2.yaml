# Accountant-aligned v2 real-data feasibility grid. These configs use fixed
# public Poisson normalization and fixed public 4x4 pooling. They replace the
# pre-v2 generated grid, which must not be used for result claims.
base_config: configs/phase1_cifar10_binary_cpu.yaml
target_epsilons: [2.0, 8.0, inf]
heterogeneity: [0.20, 0.75]
deletion_client_ids: [2]
seeds: [20260920, 20260921, 20260922, 20260923, 20260924]
execution_order: epsilon_then_heterogeneity_then_seed
note: >
  CIFAR-10 binary public-data feasibility grid; accountant-aligned v2 mechanism.
  This grid is not a multi-class SOTA benchmark and does not support a paper claim
  until published baselines, stronger attacks, and confirmation studies complete.

