# Frozen before execution. Grid generation calibrates a noise multiplier for each
# finite target epsilon using the same client-sampling RDP accountant as the run.
base_config: configs/phase1_cifar10_binary_cpu.yaml
target_epsilons: [2.0, 8.0, inf]
heterogeneity: [0.20, 0.75]
deletion_client_ids: [2]
seeds: [20260830, 20260831, 20260832, 20260833, 20260834]
execution_order: epsilon_then_heterogeneity_then_seed
note: >
  This is a real-data CPU pilot grid, not the final multi-class paper grid.
  Freeze generated config hashes before running and do not tune on attack outputs.

